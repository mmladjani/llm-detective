# Environment variable reference. Export these in the shell that starts the app;
# .env files are not loaded automatically. Never commit a real key.
# Rule-based (offline) mode needs none of these.

ANTHROPIC_API_KEY=
# Optional; defaults to claude-sonnet-4-5
ANTHROPIC_MODEL=claude-sonnet-4-5
